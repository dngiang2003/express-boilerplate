const COOKIE_NAME = 'lang';

const LOCALES = ['vi', 'en'];

const LANGUAGE_DEFAULT = 'vi';

const HEADER_NAME = 'accept-language';

module.exports = {
  LOCALES,
  HEADER_NAME,
  COOKIE_NAME,
  LANGUAGE_DEFAULT,
};
